# ── Database ──────────────────────────────────────────────────────────────────
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/<dbname>?retryWrites=true&w=majority

# ── JWT Auth ───────────────────────────────────────────────────────────────────
SECRET_STR=replace_with_a_long_random_secret
LOGIN_EXPIRE=2592000        # token lifetime in seconds (30 days)

# ── Server ────────────────────────────────────────────────────────────────────
SERVER_PORT=3000
SERVER_BASE_URL=http://localhost:3000  # complete base URL (include port for dev; omit port for prod e.g. https://api.example.com)
CLIENT_PORT=5173               # frontend dev port (used in reset-password URL construction)

# ── CORS ──────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins. Omit to default to http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173

# ── Gmail OAuth2 (for sending emails via nodemailer) ──────────────────────────
CLIENT_ID=your_google_oauth_client_id
CLIENT_SECRET=your_google_oauth_client_secret
REDIRECT_URI=https://developers.google.com/oauthplayground
REFRESH_TOKEN=your_google_oauth_refresh_token
SENDING_ADDRESS=your_sending_gmail_address@gmail.com

# ── Optional / Integrations ───────────────────────────────────────────────────
# EVENTBRITE_API_KEY=your_eventbrite_api_key   # only needed when using live Eventbrite data
